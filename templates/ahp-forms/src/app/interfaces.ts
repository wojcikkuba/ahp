export interface SettedData {
    name: string;
    criteria: string[];
    variants: string[];
}

export interface FormAnswer {
    criterion: string;
    varinatOne: string;
    variantTwo: string;
    count:  number;
}